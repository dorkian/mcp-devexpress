{
  "properties": [
    {
      "name": "VizTimeInterval",
      "type": "Number | Object | Enums.TimeInterval | undefined",
      "default": "undefined",
      "description": "A class describing various time intervals. Inherited by tick intervals in Chart and RangeSelector."
    },
    {
      "name": "days",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in days. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "hours",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in hours. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "milliseconds",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in milliseconds. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "minutes",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in minutes. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "months",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in months. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "quarters",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in quarters. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "seconds",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in seconds. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "weeks",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in weeks. Accepts integer values. Available only for an axis/scale that displays date-time values."
    },
    {
      "name": "years",
      "type": "Number",
      "default": null,
      "description": "Specifies the time interval measured in years. Accepts integer values. Available only for an axis/scale that displays date-time values."
    }
  ],
  "Methods": [],
  "Events": [],
  "Types": []
}